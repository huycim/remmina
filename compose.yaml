version: "3.8"
services:
  remmina:
    image: lscr.io/linuxserver/remmina:latest
    container_name: remmina
    environment:
      - PUID=1026
      - PGID=100
      - TZ=Asia/Ho_Chi_Minh
    volumes:
      - ./config:/config
    ports:
      - "3000:3000"
    restart: unless-stopped
